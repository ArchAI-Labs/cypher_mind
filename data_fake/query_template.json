[
  {
    "template_text": "get top {count} users from project {project}",
    "query_template": "MATCH (p:Person)-[:WORKS_ON]->(proj:Project {name: '{project}'}) RETURN p LIMIT {count}",
    "intent": "get_top_users_by_project",
    "parameters": [
      "count",
      "project"
    ],
    "priority": 1,
    "aliases": [
      "show first {count} users in project {project}",
      "list top {count} people from {project}"
    ],
    "parameter_patterns": {
      "count": "\\b(\\d+)\\b",
      "project": "(?:project\\s+)(?:['\"]?)([A-Za-z0-9_\\s]+?)(?:['\"]?)$"
    }
  },
  {
    "template_text": "list projects for user {user}",
    "query_template": "MATCH (p:Person {name: '{user}'})-[:WORKS_ON]->(proj:Project) RETURN proj.title AS ProjectTitle",
    "intent": "list_projects_by_user",
    "parameters": [
      "user"
    ],
    "priority": 1,
    "aliases": [
      "show projects of {user}",
      "what projects is {user} working on",
      "what projects does {user} work on"
    ],
    "parameter_patterns": {
      "user": "(?:user|for|of|is)\\s+(?:['\"]?)([A-Za-z]+(?:\\s+[A-Za-z]+)?)(?:['\"]?)(?:\\s+working|\\s+work|\\?|$)"
    }
  },
  {
    "template_text": "list all people who work for company {company}",
    "query_template": "MATCH (p:Person)-[:WORKS_FOR]->(c:Company {name: '{company}'}) RETURN p.name AS PersonName",
    "intent": "list_user_by_company",
    "parameters": [
      "company"
    ],
    "priority": 2,
    "aliases": [
      "who works at {company}",
      "all staff from {company}",
      "find people in {company}",
      "employees of {company}"
    ],
    "parameter_patterns": {
      "company": "(?:company|for|at|in|of)\\s+(?:['\"]?)([A-Za-z0-9\\s\\.\\-,&]+?)(?:['\"]?)(?:\\?|$)"
    }
  }
]